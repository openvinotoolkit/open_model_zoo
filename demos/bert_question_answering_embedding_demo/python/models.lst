# This file can be used with the --list option of the model downloader.
# For the conventional (optional) BERT model, that is used to calculate the final answer from the the contexts returned from the embedding-only model (below)
#     the demo should support any BERT calibrated/fine-tuned/distilled/etc on the SQuAD dataset, e.g. used in the basic BERT demo (../bert_question_answering_demo)
bert-*-squad-*
# For the (required) model that calculates only embeddings, the demo accepts only the following model
bert-*-squad-emb*
